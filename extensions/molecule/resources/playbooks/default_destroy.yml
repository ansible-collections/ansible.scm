---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Load the vars
      ansible.builtin.include_vars:
        file: ../../resources/vars/vars.yml

    - name: Remove the collection tmp directory
      ansible.builtin.file:
        path: "{{ molecule_collection_tmp }}"
        state: absent
