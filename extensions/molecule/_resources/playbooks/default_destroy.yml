---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove the collection tmp directory
      ansible.builtin.file:
        path: "{{ lookup('env', 'MOLECULE_COLLECTION_TMP') }}"
        state: absent
