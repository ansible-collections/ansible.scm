---
- name: Backup pattern playbook
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Confirm backup pattern works
      ansible.builtin.debug:
        msg: "Backup pattern playbook executed"
